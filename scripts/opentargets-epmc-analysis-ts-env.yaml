# conda-env create --name <env> --file <this file>
# conda activate <env>
# conda deactivate
# conda-env remove -n <env>

channels:
    - defaults
    - bioconda
    - conda-forge
dependencies:
    - python=3
    - pystan
    - fbprophet
    - pyspark
      #    - pip:
      #  - dsub
